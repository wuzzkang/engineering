# Current Goal: V2 Hero Section Options Evolution (3 Options)

## Goal Statement
Develop 3 distinct, high-conversion Hero Section options for Wuzzkang V2 Dynamic Builder:
1. **Option 1**: Modern Split Hero (`hero_split` / `hero-split`) — **COMPLETED & VERIFIED 100%**
2. **Option 2**: Centered Impact Hero (`hero_centered` / `hero-centered`) — **COMPLETED & VERIFIED 100%**
3. **Option 3**: Conversion & Video Hero (`hero_video` / `hero-video`) — **COMPLETED & VERIFIED 100%**

## Status
- **Option 1 (`hero_split`)**: Fully built, registered across all 4 repos, Unsplash 401 auth fixed, 0% forced text fallbacks in `wuzzkang-lp/script.js`.
- **Option 2 (`hero_centered`)**: Fully built, registered across all 4 repos (`wuzzkang-sections`, `wuzzkang-dashboard`, `wuzzkang-lp`, `preview-app/main.jsx`), synced via `npm run sync:templates`, and verified with 100% E2E test pass (2.44ms).
- **Option 3 (`hero_video`)**: Fully built, registered across all 4 repos (`wuzzkang-sections`, `wuzzkang-dashboard`, `wuzzkang-lp`, `preview-app/main.jsx`), with YouTube/Vimeo auto-embed, dark overlay opacity control, fallback thumbnail via `V2ImagePickerWidget`, synced via `npm run sync:templates`, and verified with 100% test pass (1.65ms).

