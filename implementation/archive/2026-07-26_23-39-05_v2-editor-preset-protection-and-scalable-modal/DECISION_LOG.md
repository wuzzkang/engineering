# Technical Decision Log — V2 Editor Preset Protection

- **DEC-001**: Nonaktifkan tombol Preset untuk proyek `status === 'deployed'` demi mencegah kerusakan konten live, serta gunakan `ConfirmDialog` danger variant untuk proyek draft yang sudah memiliki seksi.
